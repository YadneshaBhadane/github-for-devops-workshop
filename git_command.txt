ls //list of files
rm filename.extension //remove file permanatly restore using git only

git init //to initialise an empty git vcs repo (.git)
git status
git add filename.extension
git commit -m "added testing files"
git restore filename 

git remote -v //gives fetch n push url
git remote set url origin(token from github)@github.com(git remote -v url)

git push origin main